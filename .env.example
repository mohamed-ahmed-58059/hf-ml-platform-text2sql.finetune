# Copy to .env (gitignored) and fill in.
HF_TOKEN=            # HuggingFace token: gated Llama access + dataset push/fetch
OPENAI_API_KEY=      # only needed for the GPT baseline in evaluate.py
WANDB_API_KEY=       # only needed if LOG_TO_WANDB=True in train.py
